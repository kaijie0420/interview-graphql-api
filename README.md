# interview-graphql-api
### Before you start
Run `npm install` to install necessary library into the project. 

Create a .env file under the root directory. 
You may copy from .env.example.

### How to start
Run `npm start` to start the server.
